---
pageable: true
per_page: 10
title: Blog
description: Thoughts on building SaaS products and software development
layout: layout
---

<h1>Blog</h1>

<% page_articles.each_with_index do |article, i| %>
  <article class="mb-8">
    <h2 class="font-semibold"><%= link_to article.title, article, class: "text-white no-underline hover:underline" %></h2>
    <p><time class="text-gray-border"><%= article.date.strftime('%B %e, %Y') %></time></p>
  </article>
<% end %>

<% if paginate %>
  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>